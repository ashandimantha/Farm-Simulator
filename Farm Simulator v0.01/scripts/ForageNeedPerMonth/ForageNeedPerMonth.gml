/// @description Calculating Forage need for a month.
/// @argument0 Animals

animals = argument0;
average_weight = 1200
monthly_utilization_rate = 1.2;

return (monthly_utilization_rate) * (animals) * (average_weight);